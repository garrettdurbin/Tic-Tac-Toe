makeJson('product_table');
        function makeJson(string $table){
          $result = mysqli_query($db, "SELECT * FROM $table");
          $data = array();
          while ($row=mysqli_fetch_assoc($result)) {
            array_push($data, $row);
            unset($row);
          }
          echo json_encode($data);
          
          $json_data = json_encode($data);
          file_put_contents($table + '.json', $json_data);
        };


function productsPage(){
  //productsPage function code goes here.
  
  productInfoPage()
}
function productInfoPage(){
  //productInfoPage function code goes here.
}


// Code that's actually running:
productspage()